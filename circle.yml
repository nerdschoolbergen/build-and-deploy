dependencies:
  override:
    - npm install
    - bower install
  post:
    - grunt copy

test:
  override:
    - grunt karma:unit

deployment:
  prod:
    branch: master
    heroku:
      appname: nerdschool-deploy