dependencies:
  override:
    - npm install
    - bower install
  post:
    - grunt copy

test:
  override:
    - grunt test
#    - grunt lint

deployment:
  prod:
    branch: master
    heroku:
      appname: nerdschool-deploy